# Shell scripts must always be LF-terminated regardless of platform —
# bash chokes on CRLF, and `core.autocrlf=true` would otherwise rewrite
# them on Windows checkouts.
*.sh text eol=lf

# GitHub Actions workflows must be LF; the runner refuses CRLF.
*.yml text eol=lf
*.yaml text eol=lf
